# 21_annotation

## 功能概述
这一部分用于基因注释和基因名转移，既包含基于 GeMoMa 的注释，也包含基于 BLAST 的 FASTA 基因命名。

## 子目录说明
- `Genoma`：运行 GeMoMa 做注释
- `annotate_fasta_genes`：对 pep / cds 文件做基因名标注

## 使用建议
请优先进入具体子目录阅读对应的 `README.md`，再修改脚本顶部配置并运行。
